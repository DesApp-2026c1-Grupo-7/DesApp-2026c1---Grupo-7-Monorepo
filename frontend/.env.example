# Copiar a .env y completar. NO subir .env a git.

# URL del backend
VITE_API_URL=http://localhost:5000/api

# Login con Google (OAuth). El Client ID NO es secreto (va en el bundle), queda acá
# para que el equipo pueda probar el login con Google.
# Debe ser el MISMO valor que GOOGLE_CLIENT_ID en el backend.
# Si no se define, el botón de Google no aparece y queda solo el login con email/password.
VITE_GOOGLE_CLIENT_ID=385273852557-jugmj7hque17c05froav3b792rmlpuit.apps.googleusercontent.com
